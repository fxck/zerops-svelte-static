zerops:
  - setup: svelte
    build:
      base: nodejs@20
      buildCommands:
        - npm i
        - npm run build
      deploy:
        - 'build/~'
      cache:
        - node_modules
        - package-lock.json